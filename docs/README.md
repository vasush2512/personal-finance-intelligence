Put screenshots here.

The main README links to `docs/dashboard.png` and `docs/transactions.png`.
Run the app, take the two screenshots, save them under those names, and the
images at the top of the README start working.
