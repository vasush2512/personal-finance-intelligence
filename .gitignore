# Python
venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Generated data — regenerate with scripts/make_sample.py or by uploading a CSV
backend/data/expenses.db
backend/data/model.joblib

# Never commit a real bank statement
backend/data/real_*.csv

# Frontend
node_modules/
dist/

# Local environment overrides. frontend/.env.example is committed; the real
# values are not, because they differ per machine and per deployment.
.env
.env.local
.env.*.local

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Vite build output
frontend/dist/

# Working notes, kept locally rather than published
CLAUDE.md
PRD.md
